
app.constant('APIConfig', {
  url: 'http://127.0.0.1:9000/api/',          #  Change in prodution
});


app.constant('URLTemplates', (function() {
  var debug = true;						      #  'false' to change the template urls in the angular application
	if (debug)                                #  to server the app with django.	
		return '';

	return 'static/';
})());
